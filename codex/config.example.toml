# Codex writes account, plugin, cache and trusted-project state into the live
# ~/.codex/config.toml, so this is a template rather than a symlink target.

model_reasoning_effort = "high"
personality = "pragmatic"

[features]
memories = true

[tui]
status_line = ["model-with-reasoning", "current-dir", "git-branch", "context-used", "five-hour-limit", "weekly-limit"]
